---
label: team
hide_body: true
fields:
- type: text
  name: layout
  label: layout
- type: text
  name: title
  label: title
- name: editor-in-chief
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: Editor-in-Chief
- name: editorial
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: Editorial
- name: design_team
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: Design
- name: events
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: Events
- name: public_relations
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: Public Relations
- name: media
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: Media
- name: finance
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: Finance
pages:
- team.md
